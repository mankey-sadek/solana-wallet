{
  "name": "solana-copy-trader",
  "version": "1.0.0",
  "description": "Copy-trading bot that mirrors a target Solana wallet's swaps with a configurable position size and mirrors position closes.",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "scan": "ts-node src/scanWallet.ts",
    "build": "tsc -p tsconfig.json && node -e \"require('fs').cpSync('src/web/public','dist/web/public',{recursive:true})\"",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@solana/web3.js": "^1.95.3",
    "bs58": "^5.0.0",
    "dotenv": "^16.4.5",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^20.14.9",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18"
  }
}
